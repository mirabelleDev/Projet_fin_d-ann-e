dependencies:
  flutter:
    sdk: flutter

  # Gestion d'état (Bloc/Cubit)
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # Navigation
  go_router: ^10.0.0

  # HTTP et stockage
  dio: ^5.3.2
  shared_preferences: ^2.2.2

  # Utilitaires (UUID, Dates)
  uuid: ^4.0.0
  intl: ^0.18.1

  # Pour les icônes et le design
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0